{
  "name": "net1062-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"dotnet run --project backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
